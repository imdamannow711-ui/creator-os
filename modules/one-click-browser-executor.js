/* DONE RITE Creator OS — One-Click Browser Executor v1.1
   iPhone-safe local preview, persistent manual trim and multi-source timed render executor.
   Originals are never overwritten. Attached voiceover is mixed independently at gain 1.
*/
(function(){
'use strict';
const VERSION='1.1';
const TRIM_KEY='done-rite-one-click-trims:v1';
const manualTrims=new Map();

function readTrimStore(){try{return JSON.parse(localStorage.getItem(TRIM_KEY)||'{}')||{};}catch(e){return{};}}
function writeTrimStore(store){try{localStorage.setItem(TRIM_KEY,JSON.stringify(store||{}));}catch(e){}}
function supports(){const canCapture=typeof HTMLCanvasElement!=='undefined'&&!!HTMLCanvasElement.prototype.captureStream,canRecord=typeof MediaRecorder!=='undefined',canDraw=typeof document!=='undefined',AudioCtx=typeof window!=='undefined'&&(window.AudioContext||window.webkitAudioContext);return {canCapture,canRecord,canDraw,canAudioMix:!!AudioCtx,localPreview:canDraw,localExport:canCapture&&canRecord&&!!AudioCtx};}
function pickMime(){if(typeof MediaRecorder==='undefined'||typeof MediaRecorder.isTypeSupported!=='function')return'';const choices=['video/mp4;codecs=avc1.42E01E,mp4a.40.2','video/mp4','video/webm;codecs=vp9,opus','video/webm;codecs=vp8,opus','video/webm'];for(const m of choices){try{if(MediaRecorder.isTypeSupported(m))return m;}catch(e){}}return'';}
function createPreviewUrl(file){if(!file)throw new Error('Missing source file.');return URL.createObjectURL(file);}
function release(url){if(url)try{URL.revokeObjectURL(url);}catch(e){}}
function buildExecutionState(session,recipe){const caps=supports(),mime=pickMime();return {engine:'DONE RITE One-Click Browser Executor',version:VERSION,status:caps.localExport&&mime?'LOCAL_EXPORT_AVAILABLE':'PREVIEW_ONLY',sessionId:session&&session.id||null,originalPreserved:true,recipe:recipe||null,capabilities:caps,recorderMime:mime||null,exportWarning:caps.localExport&&mime?null:'This browser can preview the planned edit but cannot reliably encode the final local video in this path.',requiresUserGestureForSave:true};}
function waitEvent(target,name,timeout){return new Promise((resolve,reject)=>{let timer=null;const done=()=>{cleanup();resolve();},fail=()=>{cleanup();reject(new Error('Media '+name+' failed.'));};function cleanup(){target.removeEventListener(name,done);target.removeEventListener('error',fail);if(timer)clearTimeout(timer);}target.addEventListener(name,done,{once:true});target.addEventListener('error',fail,{once:true});if(timeout)timer=setTimeout(()=>{cleanup();reject(new Error('Timed out waiting for media '+name+'.'));},timeout);});}
function waitUntil(test,timeout,label){return new Promise((resolve,reject)=>{const started=Date.now();function tick(){let ok=false;try{ok=!!test();}catch(e){}if(ok)return resolve();if(Date.now()-started>=timeout)return reject(new Error('Timed out waiting for '+label+'.'));setTimeout(tick,80);}tick();});}
function waitRecorderState(recorder,eventName,timeout){return new Promise((resolve,reject)=>{let timer=null;const done=()=>{cleanup();resolve();},fail=e=>{cleanup();reject(e&&e.error||new Error('Recorder '+eventName+' failed.'));};function cleanup(){recorder.removeEventListener(eventName,done);recorder.removeEventListener('error',fail);if(timer)clearTimeout(timer);}recorder.addEventListener(eventName,done,{once:true});recorder.addEventListener('error',fail,{once:true});timer=setTimeout(()=>{cleanup();reject(new Error('Timed out waiting for recorder '+eventName+'.'));},timeout||5000);});}
function fileKey(file,index){return [index||0,file&&file.name||'',file&&file.size||0,file&&file.lastModified||0].join('|');}
function formatTime(sec){sec=Math.max(0,Number(sec||0));const m=Math.floor(sec/60),s=Math.floor(sec%60),t=Math.floor((sec-Math.floor(sec))*10);return String(m).padStart(2,'0')+':'+String(s).padStart(2,'0')+'.'+t;}
function getManualTrim(file,index){const key=fileKey(file,index);if(manualTrims.has(key))return manualTrims.get(key);const saved=readTrimStore()[key];if(saved&&Number.isFinite(Number(saved.start))&&Number.isFinite(Number(saved.end))){manualTrims.set(key,saved);return saved;}return null;}
function setManualTrim(file,index,start,end,duration){const d=Math.max(.1,Number(duration||end||0)),minGap=Math.min(.25,d);start=Math.max(0,Math.min(Number(start||0),d-minGap));end=Math.max(start+minGap,Math.min(Number(end||d),d));const changed=start>.05||end<d-.05,value={sourceIndex:index||0,start:+start.toFixed(3),end:+end.toFixed(3),duration:+d.toFixed(3),changed,fileName:file&&file.name||'',size:file&&file.size||0,lastModified:file&&file.lastModified||0,updatedAt:Date.now()},key=fileKey(file,index);manualTrims.set(key,value);const store=readTrimStore();store[key]=value;writeTrimStore(store);return value;}
function clearManualTrims(options){manualTrims.clear();if(options&&options.persistent){try{localStorage.removeItem(TRIM_KEY);}catch(e){}}}
async function loadPlayableVideo(file){const video=document.createElement('video');video.playsInline=true;video.preload='auto';video.volume=1;video.muted=false;const url=URL.createObjectURL(file);video.src=url;video.load();try{if(video.readyState<1)await waitEvent(video,'loadedmetadata',20000);return {video,url};}catch(err){release(url);throw err;}}
async function ensureFrameReady(video,targetTime){const target=Math.max(0,Math.min(Number(targetTime||0),Math.max(0,(video.duration||0)-.02)));if(Math.abs(video.currentTime-target)>.04){video.currentTime=target;try{await waitEvent(video,'seeked',20000);}catch(err){if(video.readyState<2)throw err;}}if(video.readyState>=2)return;const wasMuted=video.muted;video.muted=true;try{try{await video.play();}catch(e){}await waitUntil(()=>video.readyState>=2,30000,'video frame data');}finally{try{video.pause();}catch(e){}video.muted=wasMuted;}}
function makeCanvasForVideo(video,maxLongEdge){const vw=video.videoWidth||1080,vh=video.videoHeight||1920,maxEdge=Math.max(vw,vh),limit=Math.max(360,Number(maxLongEdge||1280)),scale=maxEdge>limit?limit/maxEdge:1,canvas=document.createElement('canvas');canvas.width=Math.max(2,Math.round(vw*scale));canvas.height=Math.max(2,Math.round(vh*scale));return canvas;}
function fitContain(ctx,video,w,h){const vw=video.videoWidth||w,vh=video.videoHeight||h,scale=Math.min(w/vw,h/vh),dw=vw*scale,dh=vh*scale,x=(w-dw)/2,y=(h-dh)/2;ctx.fillStyle='#000';ctx.fillRect(0,0,w,h);if(video.readyState>=2)ctx.drawImage(video,x,y,dw,dh);}
function wrapText(ctx,text,maxWidth){const words=String(text||'').trim().split(/\s+/),lines=[];let line='';for(const word of words){const test=line?line+' '+word:word;if(ctx.measureText(test).width>maxWidth&&line){lines.push(line);line=word;}else line=test;}if(line)lines.push(line);return lines.slice(0,3);}
function drawOverlay(ctx,overlay,w,h){if(!overlay||!overlay.text)return;const fontSize=Math.max(26,Math.round(w*.058)),x=Math.round(w*.08),y=Math.round(h*.17),maxWidth=Math.round(w*.78);ctx.font='900 '+fontSize+'px -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif';ctx.textBaseline='top';ctx.lineJoin='round';const lines=wrapText(ctx,String(overlay.text).slice(0,100),maxWidth),gap=Math.round(fontSize*1.12);lines.forEach((line,i)=>{ctx.lineWidth=Math.max(3,Math.round(w*.005));ctx.strokeStyle='rgba(0,0,0,.92)';ctx.fillStyle='#fff';ctx.strokeText(line,x,y+i*gap);ctx.fillText(line,x,y+i*gap);});}
function overlayForTime(recipe,t){const overlays=recipe&&Array.isArray(recipe.overlays)?recipe.overlays:[],d=Math.max(.1,Number(recipe&&recipe.outputDurationSeconds||0));if(!overlays.length)return null;if(overlays.length===1)return overlays[0];if(t<Math.min(2,d*.25))return overlays[0];if(overlays.length>2&&t>=Math.max(0,d-2.4))return overlays[overlays.length-1];if(overlays.length>1&&t>=d*.34&&t<=d*.72)return overlays[Math.min(1,overlays.length-1)];return null;}
function drawUntil(video,ctx,canvas,cut,baseElapsed,recipe,onProgress){return new Promise((resolve,reject)=>{let stopped=false;function frame(){if(stopped)return;try{if(video.readyState>=2){fitContain(ctx,video,canvas.width,canvas.height);const elapsed=baseElapsed+Math.max(0,Math.min(cut.end,video.currentTime)-cut.start);drawOverlay(ctx,overlayForTime(recipe,elapsed),canvas.width,canvas.height);if(typeof onProgress==='function')onProgress(Math.min(1,elapsed/Math.max(.1,recipe.outputDurationSeconds||1)));}if(video.currentTime>=cut.end-.025||video.ended){stopped=true;resolve();return;}requestAnimationFrame(frame);}catch(err){stopped=true;reject(err);}}requestAnimationFrame(frame);});}
async function previewFrame(file,recipe,options){options=options||{};if(!file)throw new Error('Missing source file.');const loaded=await loadPlayableVideo(file),video=loaded.video,url=loaded.url;try{const cuts=recipe&&Array.isArray(recipe.selectedCuts)?recipe.selectedCuts:[],first=cuts[0],target=first?Math.min(first.end-.05,first.start+Math.min(.35,Math.max(.08,(first.end-first.start)*.15))):Math.min(.35,Math.max(.08,(video.duration||1)*.08));await ensureFrameReady(video,target);const canvas=makeCanvasForVideo(video,options.maxLongEdge||1280),ctx=canvas.getContext('2d');fitContain(ctx,video,canvas.width,canvas.height);drawOverlay(ctx,overlayForTime(recipe,0),canvas.width,canvas.height);return {canvas,dataUrl:canvas.toDataURL('image/jpeg',.9),sourceTime:target};}finally{video.pause();video.removeAttribute('src');video.load();release(url);}}
function createRecorder(stream,mime,options){const full={mimeType:mime,videoBitsPerSecond:Number(options.videoBitsPerSecond||6000000),audioBitsPerSecond:Number(options.audioBitsPerSecond||128000)};try{return new MediaRecorder(stream,full);}catch(e){return new MediaRecorder(stream,{mimeType:mime});}}
function applyManualTrims(files,recipeCuts){const list=Array.from(files||[]),usedManual=new Set(),out=[];(recipeCuts||[]).forEach((c,i)=>{const sourceIndex=Number.isInteger(c.sourceIndex)?c.sourceIndex:0,file=list[sourceIndex],trim=file?getManualTrim(file,sourceIndex):null;if(trim&&trim.changed){if(usedManual.has(sourceIndex))return;usedManual.add(sourceIndex);out.push({start:trim.start,end:trim.end,sourceIndex,sequenceIndex:i,manualTrim:true});return;}out.push({start:Number(c.start),end:Number(c.end),sourceIndex,sequenceIndex:i,manualTrim:false});});return out.sort((a,b)=>a.sequenceIndex-b.sequenceIndex);}
function attachedVoiceover(){try{return window.DoneRiteOneClickGapRemover&&typeof window.DoneRiteOneClickGapRemover.getRenderVoiceover==='function'?window.DoneRiteOneClickGapRemover.getRenderVoiceover():null;}catch(e){return null;}}
async function renderLocalVideo(file,recipe,options){return renderLocalBatch([file],recipe,options);}
async function renderLocalBatch(files,recipe,options){
  options=options||{};const list=Array.from(files||[]),caps=supports(),mime=pickMime(),AudioCtx=window.AudioContext||window.webkitAudioContext;
  if(!list.length)throw new Error('Missing source files.');if(!recipe||!Array.isArray(recipe.selectedCuts)||!recipe.selectedCuts.length)throw new Error('No selected cuts to render.');if(!caps.localExport||!mime||!AudioCtx)throw new Error('Local video export is not available in this browser.');
  const cuts=applyManualTrims(list,recipe.selectedCuts).filter(c=>c.end>c.start);if(!cuts.length)throw new Error('No usable selected range to render.');
  const firstIndex=Math.max(0,Math.min(list.length-1,cuts[0].sourceIndex)),renderDuration=cuts.reduce((n,c)=>n+Math.max(0,c.end-c.start),0),renderRecipe=Object.assign({},recipe,{outputDurationSeconds:renderDuration,selectedCuts:cuts}),voiceSpec=attachedVoiceover();
  let audioCtx=null,canvasStream=null,combined=null,recorder=null,outputUrl='',video=null,currentUrl='',currentSourceIndex=-1,voiceAudio=null,voiceUrl='';
  try{
    video=document.createElement('video');video.playsInline=true;video.preload='auto';video.volume=1;video.muted=false;currentUrl=URL.createObjectURL(list[firstIndex]);video.src=currentUrl;video.load();
    audioCtx=new AudioCtx();await Promise.allSettled([audioCtx.state==='running'?Promise.resolve():audioCtx.resume()]);if(video.readyState<1)await waitEvent(video,'loadedmetadata',20000);currentSourceIndex=firstIndex;await ensureFrameReady(video,cuts[0].start);
    const canvas=makeCanvasForVideo(video,options.maxLongEdge||1280),ctx=canvas.getContext('2d',{alpha:false}),dest=audioCtx.createMediaStreamDestination(),sourceNode=audioCtx.createMediaElementSource(video),gain=audioCtx.createGain();gain.gain.value=1;sourceNode.connect(gain);gain.connect(dest);
    if(voiceSpec&&voiceSpec.blob){voiceAudio=document.createElement('audio');voiceAudio.preload='auto';voiceAudio.volume=1;voiceAudio.muted=false;voiceUrl=URL.createObjectURL(voiceSpec.blob);voiceAudio.src=voiceUrl;voiceAudio.load();if(voiceAudio.readyState<1)await waitEvent(voiceAudio,'loadedmetadata',20000);const voiceNode=audioCtx.createMediaElementSource(voiceAudio),voiceGain=audioCtx.createGain();voiceGain.gain.value=1;voiceNode.connect(voiceGain);voiceGain.connect(dest);}
    canvasStream=canvas.captureStream(Number(options.fps||30));combined=new MediaStream([...canvasStream.getVideoTracks(),...dest.stream.getAudioTracks()]);const chunks=[];recorder=createRecorder(combined,mime,options);recorder.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data);};const stopped=new Promise((resolve,reject)=>{recorder.onstop=resolve;recorder.onerror=e=>reject(e.error||new Error('MediaRecorder failed.'));});
    async function switchSource(sourceIndex,startTime){sourceIndex=Math.max(0,Math.min(list.length-1,Number(sourceIndex||0)));if(currentSourceIndex!==sourceIndex){try{video.pause();}catch(e){}release(currentUrl);currentUrl=URL.createObjectURL(list[sourceIndex]);video.src=currentUrl;video.load();if(video.readyState<1)await waitEvent(video,'loadedmetadata',20000);currentSourceIndex=sourceIndex;}await ensureFrameReady(video,startTime||0);}
    await switchSource(firstIndex,cuts[0].start);fitContain(ctx,video,canvas.width,canvas.height);drawOverlay(ctx,overlayForTime(renderRecipe,0),canvas.width,canvas.height);recorder.start(250);let baseElapsed=0;
    for(let i=0;i<cuts.length;i++){
      const cut=cuts[i];
      if(i>0){if(voiceAudio)voiceAudio.pause();if(recorder.state==='recording'){const p=waitRecorderState(recorder,'pause',5000);recorder.pause();await p;}await switchSource(cut.sourceIndex,cut.start);if(recorder.state==='paused'){const r=waitRecorderState(recorder,'resume',5000);recorder.resume();await r;}if(voiceAudio&&!voiceAudio.ended){try{await voiceAudio.play();}catch(err){throw new Error('Safari blocked the attached voiceover during render. Render stopped instead of silently omitting it.');}}}
      else if(voiceAudio){voiceAudio.currentTime=0;try{await voiceAudio.play();}catch(err){throw new Error('Safari blocked the attached voiceover during render. Render stopped instead of silently omitting it.');}}
      video.muted=false;video.volume=1;try{await video.play();}catch(err){if(voiceAudio)voiceAudio.pause();throw new Error('Safari blocked unmuted playback for a selected cut. Render stopped instead of silently removing the original voice.');}
      await drawUntil(video,ctx,canvas,cut,baseElapsed,renderRecipe,options.onProgress);video.pause();baseElapsed+=cut.end-cut.start;
    }
    if(voiceAudio)voiceAudio.pause();if(recorder.state!=='inactive')recorder.stop();await stopped;const blob=new Blob(chunks,{type:mime.split(';')[0]||'video/mp4'});if(!blob.size)throw new Error('The browser returned an empty rendered video.');outputUrl=URL.createObjectURL(blob);return {status:'RENDER_COMPLETE',blob,url:outputUrl,mimeType:blob.type||mime,fileName:recipe.export&&recipe.export.fileName||'done-rite-ad.mp4',durationSeconds:+baseElapsed.toFixed(2),width:canvas.width,height:canvas.height,sourceCount:new Set(cuts.map(c=>c.sourceIndex)).size,manualTrimUsed:cuts.some(c=>c.manualTrim),voiceoverAttached:!!(voiceSpec&&voiceSpec.blob),voiceoverName:voiceSpec&&voiceSpec.name||null,originalPreserved:true};
  }catch(err){if(outputUrl)release(outputUrl);throw err;}finally{try{if(video){video.pause();video.removeAttribute('src');video.load();}}catch(e){}try{if(voiceAudio){voiceAudio.pause();voiceAudio.removeAttribute('src');voiceAudio.load();}}catch(e){}release(currentUrl);release(voiceUrl);try{if(recorder&&recorder.state!=='inactive')recorder.stop();}catch(e){}try{if(canvasStream)canvasStream.getTracks().forEach(t=>t.stop());}catch(e){}try{if(combined)combined.getTracks().forEach(t=>t.stop());}catch(e){}try{if(audioCtx)await audioCtx.close();}catch(e){}}
}

function installMultiClipReviewUI(){
  const input=document.getElementById('video'),preview=document.getElementById('preview');if(!input||!preview||document.getElementById('doneRiteClipReview'))return;
  const wrap=document.createElement('div');wrap.id='doneRiteClipReview';wrap.style.display='none';wrap.style.marginTop='10px';
  wrap.innerHTML=''<
    +'<div id="doneRiteClipCounter" style="text-align:center;font-weight:800;margin-bottom:8px;color:#c4ccd6"></div>'
    +'<div id="doneRiteTrimPanel" style="background:#171c25;border:1px solid #2a3442;border-radius:14px;padding:12px;margin:8px 0 10px">'
    +'<div style="display:flex;justify-content:space-between;gap:8px;font-size:12px;font-weight:900;color:#c4ccd6"><span>START <b id="doneRiteStartTime" style="color:#58a6ff">00:00.0</b></span><span>END <b id="doneRiteEndTime" style="color:#58a6ff">00:00.0</b></span></div>'
    +'<div id="doneRiteTrimTrack" style="position:relative;height:54px;margin:8px 4px;touch-action:pan-y"><div style="position:absolute;left:0;right:0;top:22px;height:10px;border-radius:999px;background:#2a3442"></div><div id="doneRiteTrimFill" style="position:absolute;top:22px;height:10px;border-radius:999px;background:#1e7bff"></div><button id="doneRiteStartHandle" type="button" aria-label="Trim start" style="position:absolute;top:8px;width:38px;height:38px;border-radius:50%;border:2px solid #fff;background:#1e7bff;transform:translateX(-19px);padding:0"></button><button id="doneRiteEndHandle" type="button" aria-label="Trim end" style="position:absolute;top:8px;width:38px;height:38px;border-radius:50%;border:2px solid #fff;background:#1e7bff;transform:translateX(-19px);padding:0"></button></div>'
    +'<div id="doneRiteTrimDuration" style="text-align:center;color:#8793a1;font-size:13px;margin-bottom:8px">Selected: full clip</div><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><button id="doneRitePreviewTrim" type="button" style="min-height:44px;border-radius:12px;border:1px solid #58a6ff;background:#171c25;color:#58a6ff;font-weight:900">▶ PREVIEW SELECTED</button><button id="doneRiteResetTrim" type="button" style="min-height:44px;border-radius:12px;border:1px solid #2a3442;background:#171c25;color:#c4ccd6;font-weight:900">RESET FULL CLIP</button></div></div>'
    +'<div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><button id="doneRitePrevClip" type="button" style="min-height:44px;border-radius:12px;border:1px solid #2a3442;background:#171c25;color:#58a6ff;font-weight:900">◀ PREVIOUS CLIP</button><button id="doneRiteNextClip" type="button" style="min-height:44px;border-radius:12px;border:1px solid #2a3442;background:#171c25;color:#58a6ff;font-weight:900">NEXT CLIP ▶</button></div>';
  preview.insertAdjacentElement('afterend',wrap);
  let index=0,url='',duration=0,start=0,end=0,previewStopHandler=null,loadToken=0;
  const counter=wrap.querySelector('#doneRiteClipCounter'),prev=wrap.querySelector('#doneRitePrevClip'),next=wrap.querySelector('#doneRiteNextClip'),track=wrap.querySelector('#doneRiteTrimTrack'),fill=wrap.querySelector('#doneRiteTrimFill'),startHandle=wrap.querySelector('#doneRiteStartHandle'),endHandle=wrap.querySelector('#doneRiteEndHandle'),startTime=wrap.querySelector('#doneRiteStartTime'),endTime=wrap.querySelector('#doneRiteEndTime'),durationText=wrap.querySelector('#doneRiteTrimDuration'),previewTrim=wrap.querySelector('#doneRitePreviewTrim'),resetTrim=wrap.querySelector('#doneRiteResetTrim');
  function list(){return Array.from(input.files||[]);}function percent(t){return duration>0?Math.max(0,Math.min(100,t/duration*100)):0;}
  function updateTrimUI(){const ps=percent(start),pe=percent(end);startHandle.style.left=ps+'%';endHandle.style.left=pe+'%';fill.style.left=ps+'%';fill.style.width=Math.max(0,pe-ps)+'%';startTime.textContent=formatTime(start);endTime.textContent=formatTime(end);durationText.textContent=duration>0?'Selected: '+formatTime(Math.max(0,end-start))+(start>.05||end<duration-.05?' • SAVED MANUAL RANGE':' • full clip'):'Loading clip timing…';previewTrim.disabled=!duration;resetTrim.disabled=!duration;}
  function saveTrim(){const files=list();if(!files[index]||!duration)return;setManualTrim(files[index],index,start,end,duration);updateTrimUI();}
  function loadTrim(){const files=list(),saved=files[index]?getManualTrim(files[index],index):null;start=saved?Math.min(Number(saved.start)||0,duration):0;end=saved?Math.min(Number(saved.end)||duration,duration):duration;if(end<=start)end=duration;updateTrimUI();}
  function positionFromEvent(e){const r=track.getBoundingClientRect();return Math.max(0,Math.min(duration,((e.clientX-r.left)/Math.max(1,r.width))*duration));}
  function bindHandle(handle,kind){handle.addEventListener('pointerdown',e=>{e.preventDefault();handle.setPointerCapture(e.pointerId);});handle.addEventListener('pointermove',e=>{if(!handle.hasPointerCapture(e.pointerId)||!duration)return;const t=positionFromEvent(e),gap=Math.min(.25,duration);if(kind==='start')start=Math.max(0,Math.min(t,end-gap));else end=Math.min(duration,Math.max(t,start+gap));saveTrim();});handle.addEventListener('pointerup',e=>{try{handle.releasePointerCapture(e.pointerId);}catch(x){}saveTrim();});}
  bindHandle(startHandle,'start');bindHandle(endHandle,'end');
  function show(i,autoPlay){const files=list();if(!files.length)return;index=Math.max(0,Math.min(files.length-1,i));const token=++loadToken;try{preview.pause();}catch(e){}if(previewStopHandler){preview.removeEventListener('timeupdate',previewStopHandler);previewStopHandler=null;}if(url)release(url);duration=0;start=0;end=0;updateTrimUI();counter.textContent='Clip '+(index+1)+' of '+files.length+' — '+files[index].name;prev.disabled=index===0;next.disabled=index===files.length-1;prev.style.opacity=prev.disabled?'.45':'1';next.style.opacity=next.disabled?'.45':'1';const onMeta=()=>{if(token!==loadToken)return;const d=Number(preview.duration);if(!Number.isFinite(d)||d<=0)return;duration=d;loadTrim();if(autoPlay)preview.play().catch(()=>{});};preview.addEventListener('loadedmetadata',onMeta,{once:true});url=createPreviewUrl(files[index]);preview.removeAttribute('src');preview.load();preview.src=url;preview.style.display='block';preview.load();}
  previewTrim.addEventListener('click',()=>{if(!duration)return;try{preview.pause();}catch(e){}preview.currentTime=Math.max(0,start);if(previewStopHandler)preview.removeEventListener('timeupdate',previewStopHandler);previewStopHandler=()=>{if(preview.currentTime>=end-.03){preview.pause();preview.removeEventListener('timeupdate',previewStopHandler);previewStopHandler=null;}};preview.addEventListener('timeupdate',previewStopHandler);preview.play().catch(()=>{});});
  resetTrim.addEventListener('click',()=>{if(!duration)return;start=0;end=duration;saveTrim();try{preview.currentTime=0;}catch(e){}});
  input.addEventListener('change',()=>setTimeout(()=>{manualTrims.clear();loadToken++;const files=list();if(!files.length){wrap.style.display='none';duration=0;start=0;end=0;updateTrimUI();if(url){release(url);url='';}return;}wrap.style.display='block';show(0,false);},0));
  prev.addEventListener('click',()=>show(index-1,false));next.addEventListener('click',()=>show(index+1,false));window.addEventListener('pagehide',()=>{loadToken++;if(url)release(url);});
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installMultiClipReviewUI,{once:true});else setTimeout(installMultiClipReviewUI,0);
function canShareFile(blob,fileName){if(!blob||typeof File==='undefined'||!navigator.share||!navigator.canShare)return false;try{return navigator.canShare({files:[new File([blob],fileName||'done-rite-ad.mp4',{type:blob.type||'video/mp4'})]});}catch(e){return false;}}
async function shareFile(blob,fileName,shareText){if(!canShareFile(blob,fileName))throw new Error('File sharing is not available in this browser.');const file=new File([blob],fileName||'done-rite-ad.mp4',{type:blob.type||'video/mp4'}),payload={files:[file],title:file.name};if(shareText)payload.text=String(shareText);await navigator.share(payload);}
window.DoneRiteOneClickBrowserExecutor={version:VERSION,trimStorageKey:TRIM_KEY,supports,pickMime,createPreviewUrl,release,buildExecutionState,previewFrame,renderLocalVideo,renderLocalBatch,canShareFile,shareFile,installMultiClipReviewUI,getManualTrim,setManualTrim,clearManualTrims,applyManualTrims};
})();
